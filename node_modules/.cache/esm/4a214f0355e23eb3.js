let server;_55e‍.w("./config/server",[["default",["server"],function(v){server=v}]]);

const PORT = process.env.PORT || 3001;

server.listen(PORT, () => {
    console.log(`app running on port ${PORT}`);
});